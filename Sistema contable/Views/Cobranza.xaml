<Page x:Class="Sistema_contable.Views.Cobranza"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="1000"
      Title="Cobranza">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Gestión de Cobranza" Style="{StaticResource TitleTextBlock}" />

        <UniformGrid Grid.Row="1" Rows="1" Columns="3" Margin="0,0,0,16">
            <Border Style="{StaticResource SummaryCard}">
                <StackPanel>
                    <TextBlock Text="Facturas Pendientes" FontSize="14" Foreground="{StaticResource TextSecondaryBrush}" />
                    <TextBlock Text="{Binding Pendientes}" FontSize="24" FontWeight="Bold" 
                               Foreground="#FF9800" Margin="0,8,0,0" />
                    <TextBlock Text="{Binding MontoPendientes, StringFormat=C}" FontSize="14" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,4,0,0" />
                </StackPanel>
            </Border>

            <Border Style="{StaticResource SummaryCard}">
                <StackPanel>
                    <TextBlock Text="Facturas Vencidas" FontSize="14" Foreground="{StaticResource TextSecondaryBrush}" />
                    <TextBlock Text="{Binding Vencidas}" FontSize="24" FontWeight="Bold" 
                               Foreground="#F44336" Margin="0,8,0,0" />
                    <TextBlock Text="{Binding MontoVencidas, StringFormat=C}" FontSize="14" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,4,0,0" />
                </StackPanel>
            </Border>

            <Border Style="{StaticResource SummaryCard}">
                <StackPanel>
                    <TextBlock Text="Cobrado este Mes" FontSize="14" Foreground="{StaticResource TextSecondaryBrush}" />
                    <TextBlock Text="{Binding Cobradas}" FontSize="24" FontWeight="Bold" 
                               Foreground="#4CAF50" Margin="0,8,0,0" />
                    <TextBlock Text="{Binding MontoCobradas, StringFormat=C}" FontSize="14" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,4,0,0" />
                </StackPanel>
            </Border>
        </UniformGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,12">
            <Button Content="Nueva Factura" Command="{Binding NuevaFacturaCommand}" Style="{StaticResource AccentButton}" Margin="0,0,6,0" />
            <Button Content="Marcar como Pagada" Command="{Binding MarcarPagadaCommand}" Margin="0,0,6,0" />
            <Button Content="Enviar Recordatorio" Command="{Binding EnviarRecordatorioCommand}" Margin="0,0,6,0" />
            <Separator Width="1" Margin="8,0" />
            <Button Content="Exportar" Command="{Binding ExportarCommand}" />
        </StackPanel>

        <GroupBox Grid.Row="3" Header="Filtros" Margin="0,0,0,12">
            <StackPanel Orientation="Horizontal">
                <TextBox Width="200" Margin="0,0,8,0">
                    <TextBox.Style>
                        <Style TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
                            <Style.Triggers>
                                <Trigger Property="Text" Value="">
                                    <Setter Property="Background">
                                        <Setter.Value>
                                            <VisualBrush Stretch="None" AlignmentX="Left">
                                                <VisualBrush.Visual>
                                                    <TextBlock Text="Buscar cliente..." 
                                                               Foreground="{StaticResource TextPlaceholderBrush}" 
                                                               Margin="8,0,0,0" />
                                                </VisualBrush.Visual>
                                            </VisualBrush>
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </TextBox.Style>
                </TextBox>
                <ComboBox Width="150" Margin="0,0,8,0">
                    <ComboBoxItem Content="Todos los estados" IsSelected="True" />
                    <ComboBoxItem Content="Pendiente" />
                    <ComboBoxItem Content="Pagada" />
                    <ComboBoxItem Content="Vencida" />
                    <ComboBoxItem Content="Anulada" />
                </ComboBox>
                <Label Content="Desde:" VerticalAlignment="Center" />
                <DatePicker Width="120" Margin="0,0,8,0" />
                <Label Content="Hasta:" VerticalAlignment="Center" />
                <DatePicker Width="120" Margin="0,0,8,0" />
                <Button Content="Aplicar" />
            </StackPanel>
        </GroupBox>

        <DataGrid Grid.Row="4" AutoGenerateColumns="False" CanUserAddRows="False" ItemsSource="{Binding Facturas}" SelectedItem="{Binding SelectedFactura, Mode=TwoWay}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Nº Factura" Width="100" Binding="{Binding NumeroFactura}" />
                <DataGridTextColumn Header="Cliente" Width="*" Binding="{Binding NombreCliente}" />
                <DataGridTextColumn Header="Fecha Emisión" Width="110" Binding="{Binding FechaEmision, StringFormat=dd/MM/yyyy}" />
                <DataGridTextColumn Header="Vencimiento" Width="110" Binding="{Binding FechaVencimiento, StringFormat=dd/MM/yyyy}" />
                <DataGridTextColumn Header="Monto" Width="120" Binding="{Binding Monto, StringFormat=N2}" />
                <DataGridTemplateColumn Header="Estado" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource StatusBadge}" Padding="6,2">
                                <TextBlock Text="{Binding Estado}" FontSize="11" />
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Días Vencido" Width="100" Binding="{Binding DiasVencido}" />
                <DataGridTemplateColumn Header="Acciones" Width="120">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <Button Content="✓" Command="{Binding DataContext.MarcarPagadaCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" Width="28" Height="28" Margin="2" ToolTip="Marcar Pagada" />
                                <Button Content="✉" Command="{Binding DataContext.EnviarRecordatorioCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" Width="28" Height="28" Margin="2" ToolTip="Enviar Recordatorio" />
                                <Button Content="👁" Command="{Binding DataContext.VerDetalleCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" Width="28" Height="28" Margin="2" ToolTip="Ver Detalle" />
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
