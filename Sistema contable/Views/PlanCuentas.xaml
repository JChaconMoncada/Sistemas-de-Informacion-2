<Page x:Class="Sistema_contable.Views.PlanCuentas"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="1000"
      Title="PlanCuentas">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="350" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.ColumnSpan="2" Text="Plan de Cuentas" Style="{StaticResource TitleTextBlock}" />

        <StackPanel Grid.Row="1" Grid.ColumnSpan="2" Orientation="Horizontal" Margin="0,0,0,12">
            <Button Content="Nueva Cuenta" Command="{Binding NuevaCuentaCommand}" Style="{StaticResource AccentButton}" Margin="0,0,6,0" />
            <Button Content="Eliminar" Command="{Binding EliminarCommand}" Margin="0,0,6,0" />
            <Separator Width="1" Margin="8,0" />
            <Button Content="Expandir Todo" Margin="0,0,6,0" />
            <Button Content="Contraer Todo" Margin="0,0,6,0" />
            <Separator Width="1" Margin="8,0" />
            <Button Content="Exportar" />
        </StackPanel>

        <GroupBox Grid.Row="2" Grid.ColumnSpan="2" Header="Filtros" Margin="0,0,0,12">
            <StackPanel Orientation="Horizontal">
                <TextBox Width="200" Margin="0,0,8,0">
                    <TextBox.Style>
                        <Style TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
                            <Style.Triggers>
                                <Trigger Property="Text" Value="">
                                    <Setter Property="Background">
                                        <Setter.Value>
                                            <VisualBrush Stretch="None" AlignmentX="Left">
                                                <VisualBrush.Visual>
                                                    <TextBlock Text="Buscar cuenta..." 
                                                               Foreground="{StaticResource TextPlaceholderBrush}" 
                                                               Margin="8,0,0,0" />
                                                </VisualBrush.Visual>
                                            </VisualBrush>
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </TextBox.Style>
                </TextBox>
                <ComboBox Width="150" Margin="0,0,8,0">
                    <ComboBoxItem Content="Todos los tipos" IsSelected="True" />
                    <ComboBoxItem Content="Activo" />
                    <ComboBoxItem Content="Pasivo" />
                    <ComboBoxItem Content="Patrimonio" />
                    <ComboBoxItem Content="Ingreso" />
                    <ComboBoxItem Content="Gasto" />
                    <ComboBoxItem Content="Costo" />
                </ComboBox>
                <ComboBox Width="120" Margin="0,0,8,0">
                    <ComboBoxItem Content="Todos niveles" IsSelected="True" />
                    <ComboBoxItem Content="Nivel 1" />
                    <ComboBoxItem Content="Nivel 2" />
                    <ComboBoxItem Content="Nivel 3" />
                    <ComboBoxItem Content="Nivel 4" />
                </ComboBox>
                <Button Content="Aplicar" />
            </StackPanel>
        </GroupBox>

        <GroupBox Grid.Row="3" Grid.Column="0" Header="Árbol de Cuentas" Margin="0,0,8,0">
            <TreeView ItemsSource="{Binding Cuentas}" x:Name="TreeCuentas" SelectedItemChanged="TreeCuentas_SelectedItemChanged">
                <TreeView.ItemTemplate>
                    <HierarchicalDataTemplate ItemsSource="{Binding Hijos}">
                        <TextBlock>
                            <Run Text="{Binding Nombre}" FontWeight="SemiBold"/>
                        </TextBlock>
                    </HierarchicalDataTemplate>
                </TreeView.ItemTemplate>
            </TreeView>
        </GroupBox>

        <GroupBox Grid.Row="3" Grid.Column="1" Header="Detalle de Cuenta" Margin="8,0,0,0">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="8">
                    <Label Content="Código:" FontWeight="SemiBold" Visibility="Collapsed" />
                    <TextBox Text="{Binding CuentaSeleccionada.Codigo, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12" Visibility="Collapsed" />

                    <Label Content="Nombre:" FontWeight="SemiBold" />
                    <TextBox Text="{Binding CuentaSeleccionada.Nombre, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12" />

                    <Label Content="Descripción Explicativa (Para Reportes):" FontWeight="SemiBold" />
                    <TextBox Text="{Binding CuentaSeleccionada.Descripcion, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12" TextWrapping="Wrap" MinLines="2" />

                    <Label Content="Tipo:" FontWeight="SemiBold" />
                    <ComboBox SelectedValue="{Binding CuentaSeleccionada.Tipo, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" SelectedValuePath="Content" Margin="0,0,0,12">
                        <ComboBoxItem Content="Activo" />
                        <ComboBoxItem Content="Pasivo" />
                        <ComboBoxItem Content="Patrimonio" />
                        <ComboBoxItem Content="Ingreso" />
                        <ComboBoxItem Content="Gasto" />
                        <ComboBoxItem Content="Costo" />
                    </ComboBox>

                    <Label Content="Nivel:" FontWeight="SemiBold" />
                    <TextBox Text="{Binding CuentaSeleccionada.Nivel, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12" />

                    <Label Content="Cuenta Padre:" FontWeight="SemiBold" />
                    <ComboBox SelectedValue="{Binding CuentaSeleccionada.CuentaPadre, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" SelectedValuePath="Codigo" ItemsSource="{Binding CuentasPadreDisponibles}" Margin="0,0,0,12">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding DisplayName}" />
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>

                    <CheckBox Content="Acepta movimiento" IsChecked="{Binding CuentaSeleccionada.AceptaMovimiento, Mode=TwoWay}" Margin="0,0,0,12" />
                    <CheckBox Content="Cuenta activa" IsChecked="{Binding CuentaSeleccionada.Activo, Mode=TwoWay}" Margin="0,0,0,12" />

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
                        <Button Content="Guardar" Command="{Binding GuardarCommand}" Style="{StaticResource AccentButton}" Margin="0,0,6,0" />
                        <Button Content="Cancelar" Command="{Binding CancelarCommand}" />
                    </StackPanel>
                </StackPanel>
            </ScrollViewer>
        </GroupBox>
    </Grid>
</Page>
