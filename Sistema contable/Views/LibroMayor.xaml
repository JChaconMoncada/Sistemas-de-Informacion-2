<Page x:Class="Sistema_contable.Views.LibroMayor"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="1000"
      Title="LibroMayor">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Libro Mayor" Style="{StaticResource TitleTextBlock}" />

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12">
            <Button Content="Ver Movimientos" Command="{Binding VerMovimientosCommand}" Style="{StaticResource AccentButton}" Margin="0,0,6,0" />
            <Button Content="Exportar PDF" Command="{Binding ExportarPdfCommand}" Margin="0,0,6,0" />
            <Button Content="Exportar Excel" Command="{Binding ExportarExcelCommand}" Margin="0,0,6,0" />
            <Separator Width="1" Margin="8,0" />
            <Button Content="Imprimir" Command="{Binding ImprimirCommand}" />
        </StackPanel>

        <GroupBox Grid.Row="2" Header="Filtros" Margin="0,0,0,12">
            <Grid Margin="8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="150" />
                    <ColumnDefinition Width="20" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="150" />
                    <ColumnDefinition Width="20" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="20" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <Label Grid.Column="0" Content="Desde:" VerticalAlignment="Center" />
                <DatePicker Grid.Column="1" SelectedDate="{Binding FechaDesde, Mode=TwoWay}" />

                <Label Grid.Column="3" Content="Hasta:" VerticalAlignment="Center" />
                <DatePicker Grid.Column="4" SelectedDate="{Binding FechaHasta, Mode=TwoWay}" />

                <Label Grid.Column="6" Content="Cuenta:" VerticalAlignment="Center" />
                <ComboBox Grid.Column="7" ItemsSource="{Binding CuentasDisponibles}"
                          SelectedItem="{Binding CuentaSeleccionada, Mode=TwoWay}">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock>
                                <TextBlock.Text>
                                    <MultiBinding StringFormat="{}{0} - {1}">
                                        <Binding Path="Codigo" />
                                        <Binding Path="Nombre" />
                                    </MultiBinding>
                                </TextBlock.Text>
                            </TextBlock>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>

                <StackPanel Grid.Column="9" Orientation="Horizontal">
                    <Button Content="Aplicar" Command="{Binding AplicarFiltroCommand}" Style="{StaticResource AccentButton}" Margin="0,0,4,0" />
                    <Button Content="Limpiar" Command="{Binding LimpiarFiltroCommand}" />
                </StackPanel>
            </Grid>
        </GroupBox>

        <DataGrid Grid.Row="3" AutoGenerateColumns="False" CanUserAddRows="False" Margin="0,0,0,12" ItemsSource="{Binding Lineas}" SelectedItem="{Binding SelectedLinea, Mode=TwoWay}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Fecha" Width="100" Binding="{Binding Fecha, StringFormat=dd/MM/yyyy}" />
                <DataGridTextColumn Header="Nº Asiento" Width="80" Binding="{Binding NumeroAsiento}" />
                <DataGridTextColumn Header="Código" Width="100" Binding="{Binding CodigoCuenta}" />
                <DataGridTextColumn Header="Cuenta" Width="*" Binding="{Binding NombreCuenta}" />
                <DataGridTextColumn Header="Descripción" Width="250" Binding="{Binding Descripcion}" />
                <DataGridTextColumn Header="Debe" Width="120" Binding="{Binding Debe, StringFormat=N2}" />
                <DataGridTextColumn Header="Haber" Width="120" Binding="{Binding Haber, StringFormat=N2}" />
                <DataGridTextColumn Header="Saldo Acumulado" Width="120" Binding="{Binding Saldo, StringFormat=N2}" />
            </DataGrid.Columns>
        </DataGrid>

        <Border Grid.Row="4" Background="{StaticResource AccentLightBrush}" 
                BorderBrush="{StaticResource AccentBrush}" BorderThickness="1" Padding="12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="120" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="1" Text="Saldo Final:" FontWeight="SemiBold" VerticalAlignment="Center" />
                <TextBlock Grid.Column="2" Text="{Binding SaldoFinal, StringFormat=C}" FontWeight="Bold" FontSize="16" 
                           Foreground="{StaticResource AccentBrush}" VerticalAlignment="Center" TextAlignment="Right" />
            </Grid>
        </Border>
    </Grid>
</Page>
