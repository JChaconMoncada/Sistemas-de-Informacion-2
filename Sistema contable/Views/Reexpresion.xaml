<Page x:Class="Sistema_contable.Views.Reexpresion"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="1000"
      Title="Reexpresion">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Reexpresión Monetaria (Ajuste por Inflación)" Style="{StaticResource TitleTextBlock}" />
        <TextBlock Grid.Row="1" Text="Asistente para ajustar los valores contables según el Índice de Precios al Consumidor (IPC) del BCV" 
                   Style="{StaticResource SubtitleTextBlock}" Margin="0,0,0,16" />

        <GroupBox Grid.Row="2" Header="Parámetros de Reexpresión" Margin="0,0,0,12">
            <Grid Margin="8">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="200" />
                    <ColumnDefinition Width="40" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="200" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Label Grid.Row="0" Grid.Column="0" Content="Fecha Origen:" VerticalAlignment="Center" />
                <DatePicker Grid.Row="0" Grid.Column="1" SelectedDate="{Binding FechaOrigen, Mode=TwoWay}" />

                <Label Grid.Row="0" Grid.Column="3" Content="Fecha Destino:" VerticalAlignment="Center" />
                <DatePicker Grid.Row="0" Grid.Column="4" SelectedDate="{Binding FechaDestino, Mode=TwoWay}" />

                <Label Grid.Row="1" Grid.Column="0" Content="IPC Origen:" VerticalAlignment="Center" Margin="0,8,0,0" />
                <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding IpcOrigen, StringFormat=N2, UpdateSourceTrigger=PropertyChanged}" Margin="0,8,0,0" />

                <Label Grid.Row="1" Grid.Column="3" Content="IPC Destino:" VerticalAlignment="Center" Margin="0,8,0,0" />
                <TextBox Grid.Row="1" Grid.Column="4" Text="{Binding IpcDestino, StringFormat=N2, UpdateSourceTrigger=PropertyChanged}" Margin="0,8,0,0" />

                <Border Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="3" 
                        Background="{StaticResource AccentLightBrush}" 
                        BorderBrush="{StaticResource AccentBrush}" 
                        BorderThickness="1" Padding="12" Margin="0,12,6,0">
                    <StackPanel>
                        <TextBlock Text="Factor de Reexpresión:" FontWeight="SemiBold" />
                        <TextBlock Text="{Binding FactorCalculado, StringFormat=N4}" FontSize="24" FontWeight="Bold" 
                                   Foreground="{StaticResource AccentBrush}" Margin="0,4,0,0" />
                        <TextBlock Text="(Destino / Origen)" FontSize="11" 
                                   Foreground="{StaticResource TextSecondaryBrush}" Margin="0,2,0,0" />
                    </StackPanel>
                </Border>
                
                <Border Grid.Row="1" Grid.Column="2" Background="{StaticResource ContentBackgroundBrush}" BorderBrush="{StaticResource ControlBorderBrush}" BorderThickness="1" CornerRadius="4" Padding="12">
                        <StackPanel>
                            <!-- Título -->
                            <TextBlock Text="Resumen de Indicadores" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,8"/>

                            <!-- IPCs -->
                            <Grid Margin="0,0,0,8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="IPC Origen:" Foreground="{StaticResource TextSecondaryBrush}" />
                                <TextBlock Grid.Column="2" Text="{Binding IpcOrigen, StringFormat=N2}" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}" />
                            </Grid>
                            
                            <Grid Margin="0,0,0,8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="IPC Destino:" Foreground="{StaticResource TextSecondaryBrush}" />
                                <TextBlock Grid.Column="2" Text="{Binding IpcDestino, StringFormat=N2}" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}" />
                            </Grid>
                            
                            <Rectangle Height="1" Fill="{StaticResource ControlBorderBrush}" Margin="0,4,0,8"/>
                            
                            <!-- Factor de Reexpresión -->
                            <Grid Margin="0,0,0,12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="Factor Aplicado:" Foreground="{StaticResource TextSecondaryBrush}" />
                                <TextBlock Grid.Column="2" Text="{Binding FactorCalculado, StringFormat=N4}" FontWeight="Bold" FontSize="14" Foreground="{StaticResource AccentDarkBrush}" />
                            </Grid>
                        </StackPanel>
                    </Border>
            </Grid>
        </GroupBox>

        <GroupBox Grid.Row="3" Header="Partidas a Reexpresar" Margin="0,0,0,12">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,8">
                    <Button Content="Actualizar Saldos" Command="{Binding ActualizarCommand}" Margin="0,0,8,0" Padding="12,4"/>
                    <Button Content="Eliminar Seleccionado" Command="{Binding EliminarPartidaCommand}" Foreground="Red" Padding="12,4"/>
                </StackPanel>
            <DataGrid Grid.Row="1" AutoGenerateColumns="False" CanUserAddRows="False" ItemsSource="{Binding Partidas}" SelectedItem="{Binding PartidaSeleccionada, Mode=TwoWay}" SelectionMode="Single" SelectionUnit="FullRow">
                <DataGrid.Columns>
                    <DataGridCheckBoxColumn Header="Aplicar" Width="60" Binding="{Binding Aplicar, UpdateSourceTrigger=PropertyChanged}" />
                    <DataGridTextColumn Header="Código" Width="100" Binding="{Binding Codigo}" />
                    <DataGridTextColumn Header="Cuenta" Width="*" Binding="{Binding Nombre}" />
                    <DataGridTextColumn Header="Tipo" Width="100" Binding="{Binding Tipo}" />
                    <DataGridTextColumn Header="Moneda" Width="80" Binding="{Binding Moneda}" />
                    <DataGridTextColumn Header="Valor Original" Width="130" Binding="{Binding ValorOriginal, StringFormat=N2}" />
                    <DataGridTextColumn Header="Factor" Width="80" Binding="{Binding Factor, StringFormat=N4}" />
                    <DataGridTextColumn Header="Valor Ajustado" Width="130" Binding="{Binding ValorAjustado, StringFormat=N2}" />
                    <DataGridTextColumn Header="Diferencia" Width="130" Binding="{Binding Diferencia, StringFormat=N2}" />
                </DataGrid.Columns>
            </DataGrid>
            </Grid>
        </GroupBox>

        <Border Grid.Row="4" Background="{StaticResource AccentLightBrush}" 
                BorderBrush="{StaticResource AccentBrush}" BorderThickness="1" 
                Padding="12" Margin="0,0,0,12">
            <StackPanel Orientation="Vertical" HorizontalAlignment="Right">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,8">
                    <TextBlock Text="{Binding PartidasSeleccionadasCount}" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,4,0"/>
                    <TextBlock Text="partidas seleccionadas. Debido a la inflación su precio inicial de " VerticalAlignment="Center" Foreground="{StaticResource TextSecondaryBrush}"/>
                    <TextBlock Text="{Binding ValorOriginalTotalMonedaFormato}" FontWeight="SemiBold" VerticalAlignment="Center"/>
                    <TextBlock Text=" subió a " VerticalAlignment="Center" Foreground="{StaticResource TextSecondaryBrush}"/>
                    <TextBlock Text="{Binding ValorAjustadoTotalMonedaFormato}" FontWeight="SemiBold" VerticalAlignment="Center"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <TextBlock Text="Ajuste Total a Registrar:" VerticalAlignment="Center" Margin="0,0,16,0" FontSize="14" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding AjusteTotalMonedaFormato}" FontWeight="Bold" FontSize="16" Foreground="{StaticResource AccentDarkBrush}" VerticalAlignment="Center" Margin="0,0,16,0"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Vista Previa Asiento" Margin="0,0,6,0" Padding="16,8" />
            <Button Content="Aplicar Reexpresión" Command="{Binding AplicarReexpresionCommand}" Style="{StaticResource AccentButton}" Margin="0,0,6,0" Padding="16,8" />
            <Button Content="Cancelar" Command="{Binding CancelarCommand}" Padding="16,8" />
        </StackPanel>
    </Grid>
</Page>
