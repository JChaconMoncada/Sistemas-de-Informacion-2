<Page x:Class="Sistema_contable.Views.Documentos"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="1000"
      Title="Documentos">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="350" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.ColumnSpan="2" Text="Control Documental" Style="{StaticResource TitleTextBlock}" />

        <StackPanel Grid.Row="1" Grid.Column="0" Orientation="Horizontal" Margin="0,0,0,12">
            <Button Content="Nuevo Documento" Style="{StaticResource AccentButton}" Margin="0,0,6,0" />
            <Button Content="Adjuntar Archivo" Margin="0,0,6,0" />
            <Button Content="Marcar Entregado" Margin="0,0,6,0" />
            <Separator Width="1" Margin="8,0" />
            <Button Content="Exportar Lista" />
        </StackPanel>

        <GroupBox Grid.Row="2" Grid.Column="0" Header="Filtros" Margin="0,0,8,12">
            <StackPanel Orientation="Horizontal">
                <TextBox Width="200" Margin="0,0,8,0">
                    <TextBox.Style>
                        <Style TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
                            <Style.Triggers>
                                <Trigger Property="Text" Value="">
                                    <Setter Property="Background">
                                        <Setter.Value>
                                            <VisualBrush Stretch="None" AlignmentX="Left">
                                                <VisualBrush.Visual>
                                                    <TextBlock Text="Buscar documento..." 
                                                               Foreground="{StaticResource TextPlaceholderBrush}" 
                                                               Margin="8,0,0,0" />
                                                </VisualBrush.Visual>
                                            </VisualBrush>
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </TextBox.Style>
                </TextBox>
                <ComboBox Width="150" Margin="0,0,8,0">
                    <ComboBoxItem Content="Todos los estados" IsSelected="True" />
                    <ComboBoxItem Content="Recibido" />
                    <ComboBoxItem Content="En Revisión" />
                    <ComboBoxItem Content="Revisado" />
                    <ComboBoxItem Content="Entregado" />
                </ComboBox>
                <ComboBox Width="150" Margin="0,0,8,0">
                    <ComboBoxItem Content="Todos los tipos" IsSelected="True" />
                    <ComboBoxItem Content="Declaración IVA" />
                    <ComboBoxItem Content="Declaración ISLR" />
                    <ComboBoxItem Content="Balance" />
                    <ComboBoxItem Content="Factura" />
                    <ComboBoxItem Content="Otro" />
                </ComboBox>
                <Button Content="Aplicar" />
            </StackPanel>
        </GroupBox>

        <DataGrid Grid.Row="3" Grid.Column="0" AutoGenerateColumns="False" CanUserAddRows="False" Margin="0,0,8,0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Width="50" Binding="{Binding Id}" />
                <DataGridTextColumn Header="Empresa" Width="*" Binding="{Binding NombreEmpresa}" />
                <DataGridTextColumn Header="Tipo" Width="150" Binding="{Binding TipoDocumento}" />
                <DataGridTextColumn Header="Descripción" Width="200" Binding="{Binding Descripcion}" />
                <DataGridTextColumn Header="Recepción" Width="100" Binding="{Binding FechaRecepcion, StringFormat=dd/MM/yyyy}" />
                <DataGridTemplateColumn Header="Estado" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource StatusBadge}" Padding="6,2">
                                <TextBlock Text="En Revisión" FontSize="11" />
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTemplateColumn Header="Acciones" Width="80">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Ver" Width="60" Height="24" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <GroupBox Grid.Row="1" Grid.RowSpan="3" Grid.Column="1" Header="Detalle del Documento" Margin="8,0,0,0">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="8">
                    <Label Content="Empresa:" FontWeight="SemiBold" />
                    <ComboBox Margin="0,0,0,12">
                        <ComboBoxItem Content="Empresa Demo 1" IsSelected="True" />
                        <ComboBoxItem Content="Empresa Demo 2" />
                    </ComboBox>

                    <Label Content="Tipo de Documento:" FontWeight="SemiBold" />
                    <ComboBox Margin="0,0,0,12">
                        <ComboBoxItem Content="Declaración IVA" IsSelected="True" />
                        <ComboBoxItem Content="Declaración ISLR" />
                        <ComboBoxItem Content="Balance" />
                        <ComboBoxItem Content="Factura" />
                        <ComboBoxItem Content="Otro" />
                    </ComboBox>

                    <Label Content="Descripción:" FontWeight="SemiBold" />
                    <TextBox TextWrapping="Wrap" AcceptsReturn="True" Height="60" Margin="0,0,0,12" />

                    <Label Content="Fecha de Recepción:" FontWeight="SemiBold" />
                    <DatePicker Margin="0,0,0,12" />

                    <Label Content="Fecha de Entrega:" FontWeight="SemiBold" />
                    <DatePicker Margin="0,0,0,12" />

                    <Label Content="Estado:" FontWeight="SemiBold" />
                    <ComboBox Margin="0,0,0,12">
                        <ComboBoxItem Content="Recibido" />
                        <ComboBoxItem Content="En Revisión" IsSelected="True" />
                        <ComboBoxItem Content="Revisado" />
                        <ComboBoxItem Content="Entregado" />
                    </ComboBox>

                    <Label Content="Observaciones:" FontWeight="SemiBold" />
                    <TextBox TextWrapping="Wrap" AcceptsReturn="True" Height="80" Margin="0,0,0,12" />

                    <Separator Margin="0,8" />

                    <Label Content="Archivo Adjunto:" FontWeight="SemiBold" />
                    <Border Background="{StaticResource ContentBackgroundBrush}" 
                            BorderBrush="{StaticResource ControlBorderBrush}" 
                            BorderThickness="1" Padding="12" Margin="0,0,0,12">
                        <StackPanel>
                            <TextBlock Text="📄 documento_ejemplo.pdf" Margin="0,0,0,4" />
                            <TextBlock Text="Tamaño: 1.2 MB" FontSize="11" 
                                       Foreground="{StaticResource TextSecondaryBrush}" />
                        </StackPanel>
                    </Border>

                    <Button Content="Adjuntar Archivo" Margin="0,0,0,8" HorizontalAlignment="Stretch" />
                    <Button Content="Ver Archivo" Margin="0,0,0,8" HorizontalAlignment="Stretch" />

                    <Separator Margin="0,8" />

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
                        <Button Content="Guardar" Style="{StaticResource AccentButton}" Margin="0,0,6,0" />
                        <Button Content="Cancelar" />
                    </StackPanel>
                </StackPanel>
            </ScrollViewer>
        </GroupBox>
    </Grid>
</Page>
