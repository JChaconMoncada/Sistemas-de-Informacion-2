<Page x:Class="Sistema_contable.Views.Movimientos"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="1000"
      Title="Movimientos Operativos">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Registro de Movimientos (Ingresos y Egresos)" Style="{StaticResource TitleTextBlock}" Margin="0,0,0,16" />

        <GroupBox Grid.Row="1" Header="Nuevo Movimiento" Margin="0,0,0,16">
            <Grid Margin="8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <!-- Fila 1: Tipo y Monto -->
                <Label Grid.Row="0" Grid.Column="0" Content="Tipo:" VerticalAlignment="Center" />
                <ComboBox Grid.Row="0" Grid.Column="1" Margin="8,4" ItemsSource="{Binding TiposTransaccion}" SelectedItem="{Binding TipoTransaccion}" />

                <Label Grid.Row="0" Grid.Column="2" Content="Monto y Moneda:" VerticalAlignment="Center" />
                <StackPanel Grid.Row="0" Grid.Column="3" Orientation="Horizontal" Margin="8,4">
                    <TextBox Width="120" Text="{Binding Monto, StringFormat=N2, UpdateSourceTrigger=PropertyChanged}" />
                    <ComboBox Width="80" Margin="8,0,0,0" ItemsSource="{Binding MonedasDisponibles}" SelectedItem="{Binding MonedaSeleccionada}" />
                </StackPanel>

                <!-- Fila 2: Fecha y Cuenta -->
                <Label Grid.Row="1" Grid.Column="0" Content="Fecha:" VerticalAlignment="Center" />
                <DatePicker Grid.Row="1" Grid.Column="1" Margin="8,4" SelectedDate="{Binding Fecha}" />

                <Label Grid.Row="1" Grid.Column="2" Content="Cuenta Destino/Origen:" VerticalAlignment="Center" />
                <ComboBox Grid.Row="1" Grid.Column="3" Margin="8,4" ItemsSource="{Binding CuentasDisponibles}" SelectedItem="{Binding CuentaSeleccionada}">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding DisplayName}" />
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>

                <!-- Fila 3: Descripción y Botón -->
                <Label Grid.Row="2" Grid.Column="0" Content="Descripción:" VerticalAlignment="Center" />
                <TextBox Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="3" Margin="8,4" Text="{Binding Descripcion, UpdateSourceTrigger=PropertyChanged}" />

                <Button Grid.Row="2" Grid.Column="4" Content="Guardar Movimiento" Command="{Binding GuardarMovimientoCommand}" Style="{StaticResource AccentButton}" Margin="16,4,0,4" Padding="16,4" />
            </Grid>
        </GroupBox>

        <GroupBox Grid.Row="2" Header="Historial de Movimientos Registrados">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,8">
                    <Button Content="Refrescar 🔄" Command="{Binding RefrescarCommand}" Margin="0,0,8,0" Padding="12,4"/>
                    <Button Content="Eliminar Seleccionado" Command="{Binding EliminarMovimientoCommand}" Foreground="Red" Padding="12,4"/>
                </StackPanel>
                <DataGrid Grid.Row="1" ItemsSource="{Binding HistorialMovimientos}" SelectedItem="{Binding ComprobanteSeleccionado, Mode=TwoWay}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" HeadersVisibility="Column" SelectionMode="Single" SelectionUnit="FullRow">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding IdComprobante}" Width="50" />
                    <DataGridTextColumn Header="Fecha" Binding="{Binding Fecha, StringFormat=dd/MM/yyyy}" Width="90" />
                    <DataGridTextColumn Header="Tipo" Binding="{Binding TipoComprobante}" Width="80" />
                    <DataGridTextColumn Header="Cuenta" Binding="{Binding CuentaAsociada}" Width="150" />
                    <DataGridTextColumn Header="Descripción" Binding="{Binding Descripcion}" Width="*" />
                    <DataGridTextColumn Header="Monto" Binding="{Binding MontoTotal, StringFormat=N2}" Width="90" />
                    <DataGridTextColumn Header="Moneda" Binding="{Binding Moneda}" Width="70" />
                    <DataGridTextColumn Header="Estado" Binding="{Binding Estado}" Width="90" />
                </DataGrid.Columns>
            </DataGrid>
            </Grid>
        </GroupBox>
    </Grid>
</Page>
