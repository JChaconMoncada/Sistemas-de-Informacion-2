<Page x:Class="Sistema_contable.Views.Comprobantes"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:sys="clr-namespace:System;assembly=mscorlib"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="1000"
      Title="Comprobantes">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Comprobante Contable" Style="{StaticResource TitleTextBlock}" />

        <GroupBox Grid.Row="1" Header="Encabezado del Asiento" Margin="0,0,0,12">
            <Grid Margin="8">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="200" />
                    <ColumnDefinition Width="20" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="150" />
                    <ColumnDefinition Width="20" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="200" />
                </Grid.ColumnDefinitions>

                <Label Grid.Row="0" Grid.Column="0" Content="Fecha:" VerticalAlignment="Center" />
                <DatePicker Grid.Row="0" Grid.Column="1" SelectedDate="{x:Static sys:DateTime.Now}" />

                <Label Grid.Row="0" Grid.Column="3" Content="Número:" VerticalAlignment="Center" />
                <TextBox Grid.Row="0" Grid.Column="4" Text="0001" IsReadOnly="True" />

                <Label Grid.Row="0" Grid.Column="6" Content="Tipo:" VerticalAlignment="Center" />
                <ComboBox Grid.Row="0" Grid.Column="7">
                    <ComboBoxItem Content="Manual" IsSelected="True" />
                    <ComboBoxItem Content="Apertura" />
                    <ComboBoxItem Content="Cierre" />
                    <ComboBoxItem Content="Ajuste" />
                    <ComboBoxItem Content="Reexpresión" />
                </ComboBox>

                <Label Grid.Row="1" Grid.Column="0" Content="Descripción:" VerticalAlignment="Center" Margin="0,8,0,0" />
                <TextBox Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="7" Margin="0,8,0,0" />
            </Grid>
        </GroupBox>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,8">
            <Button Content="+ Nueva Línea" Style="{StaticResource AccentButton}" Margin="0,0,6,0" />
            <Button Content="- Eliminar Línea" Margin="0,0,6,0" />
            <Separator Width="1" Margin="8,0" />
            <Button Content="Seleccionar Cuenta" Margin="0,0,6,0" />
        </StackPanel>

        <DataGrid Grid.Row="3" AutoGenerateColumns="False" CanUserAddRows="True" Margin="0,0,0,12">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Código" Width="100" Binding="{Binding CodigoCuenta}" />
                <DataGridTextColumn Header="Cuenta" Width="*" Binding="{Binding NombreCuenta}" />
                <DataGridTextColumn Header="Debe" Width="150" Binding="{Binding Debe, StringFormat=N2}" />
                <DataGridTextColumn Header="Haber" Width="150" Binding="{Binding Haber, StringFormat=N2}" />
                <DataGridTextColumn Header="Descripción" Width="250" Binding="{Binding Descripcion}" />
            </DataGrid.Columns>
        </DataGrid>

        <Border Grid.Row="4" Background="{StaticResource AccentLightBrush}" 
                BorderBrush="{StaticResource AccentBrush}" BorderThickness="1" 
                Padding="12" Margin="0,0,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="150" />
                    <ColumnDefinition Width="20" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="150" />
                    <ColumnDefinition Width="20" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="1" Text="Total Debe:" FontWeight="SemiBold" VerticalAlignment="Center" />
                <TextBlock Grid.Column="2" Text="Bs. 0.00" FontWeight="Bold" FontSize="16" 
                           Foreground="{StaticResource AccentBrush}" VerticalAlignment="Center" />

                <TextBlock Grid.Column="4" Text="Total Haber:" FontWeight="SemiBold" VerticalAlignment="Center" />
                <TextBlock Grid.Column="5" Text="Bs. 0.00" FontWeight="Bold" FontSize="16" 
                           Foreground="{StaticResource AccentBrush}" VerticalAlignment="Center" />

                <Border Grid.Column="7" Style="{StaticResource StatusBadgeSuccess}" Padding="12,4">
                    <TextBlock Text="✓ Cuadrado" FontWeight="SemiBold" Foreground="#4CAF50" />
                </Border>
            </Grid>
        </Border>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Guardar Asiento" Style="{StaticResource AccentButton}" Margin="0,0,6,0" Padding="16,8" />
            <Button Content="Nuevo Asiento" Margin="0,0,6,0" Padding="16,8" />
            <Button Content="Cancelar" Padding="16,8" />
        </StackPanel>
    </Grid>
</Page>
